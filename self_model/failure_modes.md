# Failure Modes

This file starts intentionally sparse. Future accepted generations may record
recurring mistakes here when train feedback supports a general diagnosis.

Reflection should update this file only for durable, reusable failure modes,
for example "premature finalization on wrapper snippets" or "overweighting a
static_scan finding." Do not store validation/test contents or memorize task
labels.
